# Backend - API de Produtos

API REST desenvolvida em Node.js com Express e Prisma (SQLite) para gerenciamento de produtos.

## Como rodar

1. Clone o repositório.
2. Instale as dependências:
   ```bash
   npm install
